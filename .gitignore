__pycache__/
*.pyc

# runtime flag log (contains dictated utterances)
flagged.md
flagged-archive.md

# personal word lists — start from the .template files
vocab.txt
corrections.txt

# secrets — API keys live here, never commit
.env
*.env
